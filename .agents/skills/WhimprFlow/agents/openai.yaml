interface:
  display_name: "WhimprFlow"
  short_description: "Repo-specific patterns and workflows for WhimprFlow"
  default_prompt: "Use the WhimprFlow repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true